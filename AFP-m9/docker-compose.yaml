version: '3.8'

services:
  flask:
    build: ./flask
    volumes:
      - document_root:/app
    ports:
      - "5000:5000"
      - "2222:22"
    networks:
      - dev
    depends_on:
      - postgres
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: torneigdb

  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: torneigdb
    volumes:
      - dbdata:/var/lib/postgresql/data
    networks:
      - dev

  developer:
    build: ./developer
    volumes:
      - document_root:/workspace
    ports:
      - "5901:5901"
      - "1222:22"
    networks:
      - dev
    depends_on:
      - postgres
      - flask

volumes:
  dbdata:
  document_root:

networks:
  dev:
    driver: bridge
